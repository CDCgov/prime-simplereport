mutation resetUserPassword (
    $id: ID!
    ){
  resetUserPassword(
    id: $id
  ) {
      id
      firstName,
      lastName,
      email,
      organization {
        name
        externalId
      }
    }
}

