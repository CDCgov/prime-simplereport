mutation SubmitTestResult(
  $patientId: ID!
  $deviceId: String!
  $results: [MultiplexResultInput]!
  $dateTested: DateTime
) {
  addMultiplexResult(
    patientId: $patientId
    deviceId: $deviceId
    results: $results
    dateTested: $dateTested
  ) {
    testResult {
      internalId
    }
  }
}
