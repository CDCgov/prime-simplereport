mutation resetUserMfa (
    $id: ID!
    ){
  resetUserMfa(
    id: $id
  ) {
      id
      firstName,
      lastName,
      email,
      organization {
        name
        externalId
      }
    }
}

