mutation resendActivationEmail (
    $id: ID!
    ){
  resendActivationEmail(
    id: $id
  ) {
      id
      firstName,
      lastName,
      email,
      organization {
        name
        externalId
      }
    }
}

