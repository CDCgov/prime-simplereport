query organization ($id: ID!){
    organization(id: $id) {
        internalId
        name
        type
        externalId
        identityVerified
        patientSelfRegistrationLink
        facilities{
            id
            name
        }
        id
    }
}