spring:
  profiles.include: no-security, no-okta-mgmt, server-debug, create-sample-data, local
  liquibase:
    user: simple_report_migrations
    password: migrations456
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: false # override this in application-local.yaml to get pretty-printed SQL all the time
        default_schema: simple_report
logging:
  level:
    gov.cdc.usds: DEBUG
twilio:
  enabled: false
