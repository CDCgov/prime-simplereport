name: Deploy Test

on:
  push:
    branches:
      - main

concurrency:
  group: test-deploy
env:
  DEPLOY_ENV: test
  NODE_VERSION: 18

jobs:
  build-cypress:
    uses: ./.github/workflows/buildCypressImage.yml
