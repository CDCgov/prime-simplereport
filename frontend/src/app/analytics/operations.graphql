query GetTopLevelDashboardMetricsNew(
  $facilityId: ID
  $startDate: DateTime
  $endDate: DateTime
) {
  topLevelDashboardMetrics(
    facilityId: $facilityId
    startDate: $startDate
    endDate: $endDate
  ) {
    positiveTestCount
    totalTestCount
  }
}
