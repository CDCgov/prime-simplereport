query GetAllOrganizations {
    organizations{
        externalId
        name
    }
}

query GetFacilitiesByOrgId($orgId: ID!) {
    organization(id: $orgId){
        facilities {
            name
            id
            isDeleted
        }
    }
}