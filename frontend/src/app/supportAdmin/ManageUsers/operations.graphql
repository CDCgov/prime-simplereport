query findUserByEmail($email: String!) {
    user(email: $email){
        id
        firstName
        middleName
        lastName
        roleDescription
        role
        permissions
        email
        status
        organization {
            testingFacility {
                id
                name
            }
        }
        isDeleted
        status
    }
}
